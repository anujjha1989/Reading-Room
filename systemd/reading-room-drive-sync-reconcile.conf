[Service]
# Replace the append-only scan with the reconciling rebuild, so books deleted
# from Drive drop out instead of accumulating as dead entries.
ExecStart=
ExecStart=/usr/local/lib/reading-room/rr-catalog-rebuild.sh
User=anujjha1989
